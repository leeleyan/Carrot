<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.LoginMapper">	
	<select id="selectItemList" parameterType="hashmap" resultType="com.example.demo.model.Member">	
		SELECT
			t2_product.board_idx, b_title, b_content, u_nickname, 
			u_address, p_price, changeDate, t2_image.img	
		FROM
			t2_product JOIN t2_image
		ON 
			t2_product.board_idx = t2_image.board_idx
	</select>
</mapper>